# Food detection stub
import cv2

def detect_food(image_path):
    # Mock: Always return one item
    return [{
        'image': image_path,  # Just pass the path for demo
        'portion': 'medium'
    }]
